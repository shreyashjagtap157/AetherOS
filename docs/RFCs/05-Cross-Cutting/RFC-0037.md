# RFC-0037: Capability Token Format

**Status:** Active.
**Implements invariants:** I-1, I-2, I-3, I-13.
**Depends on:** RFC-0007 (Execution Domain), RFC-0013 (Capability Mediation).

---

## 1. Scope

This RFC specifies the canonical binary format of a *capability token*: the unforgeable, opaque, transferable handle that authorizes operations on a resource. The capability token format is the most security-critical data structure in the entire platform — every access control decision ultimately checks a capability token described by this RFC.

This RFC implements I-1, I-2, I-3, and I-13 directly.

---

## 2. Normative Requirements

### 2.1 Capability Token Structure

`RFC-0037.1`: Every capability token has the following fixed structure:

```c
struct capability_token {
    // Identity section (32 bytes)
    uint8_t              type;                 // capability type (see 2.2)
    uint8_t              rights;               // bitmask of granted rights
    uint8_t              flags;               // capability flags (see 2.3)
    uint8_t              reserved1;
    uint32               rights_derived;       // derivation count (for debugging)
    uint64               creation_timestamp_ns; // when created (scheduling time)
    uint64               expires_at_ns;        // 0 = never; absolute scheduling time

    // Reference section (32 bytes)
    struct object_id     target_object;         // RFC-0015: the resource this cap authorizes
    uint32               target_domain_index;   // index into domain table (0 = current domain)
    uint32               reserved2;

    // Integrity section (32 bytes)
    uint8_t              seal[16];             // HMAC-SHA256 of the preceding 64 bytes
    uint8_t              reserved3[16];

    // Total: 96 bytes, 8-byte aligned
};
```

`RFC-0037.2`: Total token size is exactly 96 bytes. No field is variable-length. Any token whose total size is not 96 bytes MUST be rejected as malformed.

### 2.2 Capability Types

`RFC-0037.3`: The following capability types are defined:

| Type  | Name              | Authorized operations                           | Owning RFC / Reference |
|-------|-------------------|-----------------------------------------------|------------------------|
| 0     | `CAP_OBJECT`      | Operations on a persistent object              | `RFC-0015`             |
| 1     | `CAP_MEMORY`      | Memory region mapping (`RFC-0009`)             | `RFC-0009`, `RFC-ALLOC-001` |
| 2     | `CAP_ENDPOINT`    | Send/receive on a communication endpoint      | `RFC-0025`             |
| 3     | `CAP_DOMAIN`      | Domain creation/management (`RFC-0007`)        | `RFC-0007`             |
| 4     | `CAP_CAPABILITY`  | Transfer/revoke other capabilities            | `RFC-0015`             |
| 5     | `CAP_STORAGE`     | Storage provider operations                   | `RFC-0019`             |
| 6     | `CAP_INTERRUPT`   | Interrupt binding (`RFC-0010`)                 | `RFC-0010`             |
| 7     | `CAP_SYNC`        | Synchronization primitives (`RFC-0028`)        | `RFC-0028`             |
| 8     | `CAP_DMA_MEMORY`  | Device-originated DMA access to memory regions | `RFC-ALLOC-001 §2.3`   |
| 9     | `CAP_IOMMU_CONFIG`| IOMMU translation table programming            | `RFC-ALLOC-001 §2.4`   |
| 10    | `CAP_MMIO_REGION` | Memory-mapped I/O register access              | `RFC-ALLOC-001 §2.6`   |
| 11    | `CAP_ERROR`       | Error representation; NOT a valid capability for resource access | `RFC-COMPILER-001 §2.7` |
| 12    | `CAP_DEVICE_CONTROL` | Device register operations (read/write/status) | `RFC-DRIVER-001 §2.4` |

`RFC-0037.4`: Type-specific semantics: the meaning of the `target_object` field depends on the type. The type field MUST be checked before interpreting the `target_object`:

| Type | Target Object Interpretation |
|------|------------------------------|
| 0 (`CAP_OBJECT`) | An object identity (`object_id`, RFC-0015). |
| 1 (`CAP_MEMORY`) | A physical memory region descriptor (`phys_region_descriptor`, RFC-ALLOC-001 §2.1). |
| 2 (`CAP_ENDPOINT`) | An endpoint identifier (`endpoint_id`, RFC-0025). |
| 3 (`CAP_DOMAIN`) | A domain identifier (`domain_id`, RFC-0007 §4). |
| 4 (`CAP_CAPABILITY`) | The subject capability being operated upon (`object_id`, RFC-0015). |
| 5 (`CAP_STORAGE`) | A storage provider instance identity (`object_id`, RFC-0015 per RFC-0019). |
| 6 (`CAP_INTERRUPT`) | An interrupt source descriptor (`object_id`, RFC-0015 per RFC-0010 §4). |
| 7 (`CAP_SYNC`) | A synchronization primitive descriptor (`object_id`, RFC-0015 per RFC-0028 §4). |
| 8 (`CAP_DMA_MEMORY`) | A `dma_region_descriptor` referencing the same physical region as the parent `CAP_MEMORY` (per `RFC-ALLOC-001` §4.2) |
| 9 (`CAP_IOMMU_CONFIG`) | An IOMMU domain identifier — a `domain_id` (`RFC-0007`) referencing the IOMMU translation domain. The full IOMMU-config descriptor struct is deferred to a future RFC-ALLOC-001 Specification Amendment. For Phase 1, `domain_id` per RFC-0007 is the minimum sufficient interpretation. |
| 10 (`CAP_MMIO_REGION`) | An MMIO region identifier — an `object_id` (`RFC-0015`) referencing the MMIO region registered by the HAL at boot (per `Hardware-Support-RFC.md`). The full MMIO-region descriptor struct is deferred to a future RFC-ALLOC-001 Specification Amendment. For Phase 1, `object_id` per RFC-0015 is the minimum sufficient interpretation. |
| 11 (`CAP_ERROR`) | MUST be zero (`struct object_id` with all fields zero). Presenting a non-zero `target_object` in a `CAP_ERROR` token yields `ECAPSTALE` (malformed error token). |
| 12 (`CAP_DEVICE_CONTROL`) | A `device_id` — an `object_id` (`RFC-0015`) referencing the device registered in the device registry by the HAL at boot (per `Hardware-Support-RFC.md`). |

`RFC-0037.4.1`: For `CAP_ERROR` (type 11), the `rights` field is reinterpreted as an unsigned 8-bit error-code integer, NOT as a rights bitmask. The set of permissible error codes is defined by the authoritative platform error registry. The `ERR_*` codes at `RFC-COMPILER-001.27` are the provisional set until the registry is established. Types other than 11 interpret the `rights` field as a rights bitmask per `RFC-0037.5`.

### 2.3 Rights Flags

`RFC-0037.5`: The following rights flags are defined. A right is granted (1) or not granted (0):

| Flag position | Right name       | Meaning                                    |
|---------------|------------------|--------------------------------------------|
| 0             | `RIGHT_READ`     | Read access                                |
| 1             | `RIGHT_WRITE`    | Write access                               |
| 2             | `RIGHT_EXECUTE`  | Execute access                              |
| 3             | `RIGHT_MAP`      | Map into address space (`RFC-0009`)         |
| 4             | `RIGHT_TRANSFER` | Transfer to another holder (`RFC-0027`)     |
| 5             | `RIGHT_REVOKE`   | Revoke dependent capabilities (`RFC-0039`)    |
| 6             | `RIGHT_DERIVE`   | Derive a narrower capability               |

`RFC-0037.6`: The `rights` field is an 8-bit bitmask. `RIGHT_TRANSFER`, `RIGHT_REVOKE`, and `RIGHT_DERIVE` are collectively called *meta-rights*. Possessing meta-rights on a capability does not automatically grant any data rights; all must be explicitly granted.

### 2.4 Capability Flags

`RFC-0037.7`: The capability flags field (`flags`) defines token behaviour:

| Flag | Name               | Meaning                                               |
|------|--------------------|-------------------------------------------------------|
| 0x01 | `CAPF_TRANSIENT`    | Not stored in State; lost on domain destroy          |
| 0x02 | `CAPF_REVOCABLE`   | Subject to revocation (`RFC-0039`)                    |
| 0x04 | `CAPF_NAMED`       | Has a stable name (for lookup)                        |
| 0x08 | `CAPF_BOUND`       | Bound to a specific domain; not transferable          |
| 0x10 | `CAPF_SEALED`      | Seal is HMAC; cannot be inspected without key        |

### 2.5 Seal (Integrity)

`RFC-0037.8`: The `seal` field is an HMAC-SHA256 (16 bytes; truncated from the full 32-byte output) computed over the first 64 bytes of the token using the platform's master seal key (derived from the hardware root of trust at bootstrap). Any modification to the first 64 bytes invalidates the seal.

`RFC-0037.9`: The seal MUST be verified before any rights are enforced. A token with an invalid seal MUST be treated as non-existent. The capability subsystem does not distinguish between "no capability" and "invalid capability" — both result in `ECAPSTALE` (`RFC-0025.8`) returned to the accessor.

`RFC-0037.9.1`: For `CAP_ERROR` (type 11), the `seal` field MUST carry a valid HMAC-SHA256 seal computed over the first 64 bytes per `RFC-0037.8`. A `CAP_ERROR` token is not a valid capability for resource access; presenting it to the capability-mediator for resource access yields `ECAPSTALE` (`RFC-0025.8`, `RFC-COMPILER-001.25`). The seal ensures provenance: the error code in the `rights` field is bound to the source that generated it. A `CAP_ERROR` token with an invalid or absent seal is treated as non-existent per `RFC-0037.9` — the same treatment as any invalid capability token. Seal verification for `CAP_ERROR` tokens is required because the error code (in the `rights` field, per `RFC-COMPILER-001.27`) is data that must be integrity-protected to prevent a tampered error code from being substituted.

### 2.6 Address–Authority Separation (I-13)

`RFC-0037.10`: The `target_object` field is an *address* (a name or identifier). The rights in the `rights` field are the *authority*. These are stored in the same token but represent orthogonal concepts (I-13). The seal does not distinguish between address and authority — it seals the entire 64-byte identity block.

`RFC-0037.11`: Implementations that combine address and authority bits into a single bitfield within the seal area are non-conformant. The seal verifies the entire token; the address and authority semantics remain distinct.

### 2.7 Capability Derivation

`RFC-0037.12`: A capability with `RIGHT_DERIVE` may be used to create a new capability with a subset of the original rights (rights narrowing). The new capability's `rights_derived` counter is incremented from the original. The `creation_timestamp_ns` and `target_object` are inherited from the original.

`RFC-0037.13`: Capability derivation does NOT change the identity of the target. The derived capability authorizes the same target as the original, with narrower rights.

### 2.8 Expiration

`RFC-0037.14`: If `expires_at_ns` is non-zero and the current scheduling time exceeds it, the capability MUST be treated as invalid. Expired capabilities cannot be transferred, derived, or used for any operation.

### 2.9 Domain Binding

`RFC-0037.15`: If `CAPF_BOUND` is set, the capability is valid only within the domain identified by `target_domain_index`. A capability bound to domain A presented in domain B is invalid. This is enforced by the Capability Mediation subsystem (`RFC-0013`).

`RFC-0037.3.1`: A `CAP_ERROR` (type 11) token is not a resource-access capability. Presenting a `CAP_ERROR` token to the capability-mediator where a resource-access capability is required yields `ECAPSTALE` (`RFC-0025.8`, `RFC-COMPILER-001.25`). The mediator MUST reject the access at `CHECK_REQUEST` (`RFC-0013` §3) before evaluating rights, domain, or lifetime. This is the compensating rule for the removal of the "Type unknown" rejection that previously applied to type 11 before its recognition.

---

## 3. Failure Semantics

| Failure                  | Consequence                                               |
|--------------------------|-----------------------------------------------------------|
| Token size != 96 bytes   | `ECAPSTALE`; token rejected as malformed                   |
| Seal mismatch            | `ECAPSTALE`; token rejected as tampered                    |
| Type unknown             | `ECAPSTALE`; token rejected as malformed — does not apply to type 11 (now `CAP_ERROR`) |
| `CAP_ERROR` token used for resource access | `ECAPSTALE`; token rejected at `CHECK_REQUEST` per `RFC-0037.3.1` |
| Rights not granted       | `ECAPSTALE`; access denied                                 |
| Domain mismatch          | `ECAPSTALE`; cross-domain access denied                    |
| Expired                  | `ECAPSTALE`; expired capability                            |
| Revoked (RFC-0039)       | `ECAPSTALE`; revoked capability                            |

---

## 4. Conformance Tests

- `CTS-CAP-TOK-1`: validates RFC-0037.2 — exact 96-byte size.
- `CTS-CAP-TOK-2`: validates RFC-0037.4 — type-dependent target interpretation.
- `CTS-CAP-TOK-3`: validates RFC-0037.5 — rights bitmask semantics.
- `CTS-CAP-TOK-4`: validates RFC-0037.6 — meta-rights independence.
- `CTS-CAP-TOK-5`: validates RFC-0037.9 — seal verification before rights check.
- `CTS-CAP-TOK-6`: validates RFC-0037.10 — address/authority orthogonality (seal covers both but they remain distinct).
- `CTS-CAP-TOK-7`: validates RFC-0037.12 — derivation produces narrower rights.
- `CTS-CAP-TOK-8`: validates RFC-0037.14 — expiration enforcement.
- `CTS-CAP-TOK-9`: validates RFC-0037.15 — domain binding enforcement.
- `CTS-CAP-TOK-10`: validates RFC-0037.3 — types 8–12 are recognized by the capability-mediator as valid types; a token of each type passes `CHECK_REQUEST` (the initial mediation pipeline stage that checks type validity).
- `CTS-CAP-TOK-11`: validates RFC-0037.4.1 — a `CAP_ERROR` token's `rights` field is interpreted as an error-code integer, not as a rights bitmask; a `CAP_ERROR` token with a valid seal passes type validity but is rejected at `CHECK_REQUEST` per RFC-0037.3.1.

---

## 5. Verification Expectations

Capability token format and enforcement are Tier A. The seal algorithm is Tier A with hardware attestation.

---

## 6. Cross-References

- `RFC-0007` (Execution Domain — domain binding).
- `RFC-0013` (Capability Mediation — enforcement).
- `RFC-0015` (Object Identity — target_object for CAP_OBJECT).
- `RFC-ALLOC-001` (Allocator Hierarchy — defines types 8, 9, 10 usage; memory, DMA, IOMMU semantics).
- `RFC-COMPILER-001` (Compiler and Runtime Boundary — defines type 11 CAP_ERROR usage; error token semantics).
- `RFC-DRIVER-001` (Driver Framework Architecture — defines type 12 CAP_DEVICE_CONTROL usage; device control semantics).
- `RFC-0024` (Message Format — capability in trailer).
- `RFC-0027` (Capability Transfer — copy semantics).
- `RFC-0038` (Capability Transfer Protocol — transfer wire format).
- `RFC-0039` (Capability Revocation — revocation propagation).

---

## Revision History

| Date       | Change                                |
|------------|---------------------------------------|
| this cycle | Initial release. 96-byte fixed token. Type+rights+flags+seal. Address–authority orthogonality maintained. I-1, I-2, I-3, I-13 implemented. |
| 2026-07-20 | Specification Amendment applied (Cross-Impact Gate 001 §3.5; PHASE-4-AUTHORIZATION-RFC-0037-S2.md). Types 8–12 added to type table (WB-002, WB-009). `CAP_ERROR` (type 11) rights-field reinterpretation added (WB-005). `CAP_ERROR` mediation rejection rule added at `CHECK_REQUEST` (RFC-0037.3.1, coordinated with RFC-0013.4.1). Mandatory sealing for `CAP_ERROR` added (RFC-0037.9.1, Model A). Failure table updated with explicit `CAP_ERROR` row; "Type unknown" row annotated. Cross-references updated to include RFC-ALLOC-001, RFC-COMPILER-001, RFC-DRIVER-001. |